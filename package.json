{
  "name": "fix-in-claude-for-bugbot",
  "version": "0.1.0",
  "description": "Chrome extension: adds 'Fix in Claude' button to Cursor Bugbot comments on GitHub PRs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
